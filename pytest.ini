[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    e2e: requires PostgreSQL + Groq API (live stack)
    guardrails: requires Groq API
    api: mock-based, no external deps
